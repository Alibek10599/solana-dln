# Solana RPC endpoints (use multiple for redundancy)
# You can get free RPCs from:
# - https://www.helius.dev/ (recommended - fast & reliable)
# - https://www.quicknode.com/
# - https://www.alchemy.com/
# - Default public endpoint (rate limited)

SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Optional: Helius RPC (recommended for production - higher rate limits)
# SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_API_KEY

# ClickHouse connection
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_DATABASE=dln_dashboard
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# Dashboard API server port
API_PORT=3001

# Data collection settings
# How many orders to collect (minimum 50k as per task)
TARGET_CREATED_ORDERS=25000
TARGET_FULFILLED_ORDERS=25000

# Batch size for RPC calls (max 1000 per getSignaturesForAddress)
SIGNATURES_BATCH_SIZE=1000

# Batch size for transaction fetching
TX_BATCH_SIZE=50

# Delay between batches (ms) to avoid rate limiting
BATCH_DELAY_MS=200

# Log level (debug, info, warn, error)
LOG_LEVEL=info
